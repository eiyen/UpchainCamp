const { ethers } = require("hardhat");
const { expect } = require("chai");

// Complete the detailed test code later
// after learning the syntax of libraries of mocha, chai and ethers.js 
// and contract deployment.

describe ("Counter", function() {
    it("Check if the initial value of counter is 0.")
})